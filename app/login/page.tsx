import LoginCard from "@/components/auth/login-form"

export default async function LoginPage() {
  return (
    <main className="container min-h-screen w-full grow p-4">
      <LoginCard />
    </main>
  )
}
